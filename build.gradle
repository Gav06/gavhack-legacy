plugins {
    id 'java'
}

group 'me.gavin'
version '1.0'

dependencies {
    // add all the jars in the libs directory as a dependencies
    implementation fileTree('libs')
}

jar {
    manifest.attributes(
            'Main-Class': 'dev.gavhack.Main'
    )
}